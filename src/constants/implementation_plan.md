"# Implementation Plan - Food Delivery App (Zaika)\n\nWe will implement a complete, premium Food Delivery App with a robust nested navigation system, conditional authentication flow, deep linking, global contexts, and custom styling matching your design mockups.\n\n## Proposed Components\n\n### 1. Global Contexts (`src/context`)\n- **`AuthContext.tsx`**: Persists authentication token/user and onboarding state using `AsyncStorage`.\n- **`CartContext.tsx`**: Manages global shopping cart items, quantities, totals, and orders placed during the session.\n\n### 2. Navigation Architecture (`src/navigation`)\n- **`types.ts`**: Navigation param list types for all stack, tab, and drawer navigators.\n- **`RootNavigator.tsx`**: Handles loading state and conditionally renders the `AuthStack` or the main `DrawerNavigator`.\n- **`AuthStack.tsx`**: Includes `OnboardingScreen`, `LoginScreen`, and `SignupScreen`.\n- **`DrawerNavigator.tsx`**: Outer shell of the authenticated flow. Accessible from Profile, contains the `MainTabs` as the default screen, along with `SettingsScreen` and `HelpScreen`.\n- **`MainTabs.tsx`**: Bottom Tab Navigator with:\n  - `Home` tab (renders the nested `HomeStack`)\n  - `Search` tab (renders `SearchScreen`)\n  - `Orders` tab (renders `OrdersScreen` with badge count)\n  - `Profile` tab (renders a wrapper that triggers the drawer)\n- **`HomeStack.tsx`**: Nested stack containing:\n  - `Home` screen\n  - `RestaurantDetail` screen (bottom tab hidden)\n  - `Cart` screen (bottom tab hidden)\n  - `TrackOrder` screen (custom order tracking screen with rider info and delivery map)\n- **`linking.ts`**: Configures deep linking for `foodapp://` and `zaika://` schemes to open `restaurant/:id` directly.\n\n### 3. Screen Components (`src/screens`)\n- **`OnboardingScreen.tsx`**: Premium onboarding mockup with slide info and \"Get Started\" action.\n- **`LoginScreen.tsx`**: Sleek login page with validation and mock user login credentials.\n- **`SignupScreen.tsx`**: Signup input layout styled with our HSL tailored orange
<truncated 4288 bytes>